import React from 'react';
import Layout from '../../components/layout';

const BlogsPage = () => {
    return (
        <Layout>
            <h1>Bloggy Blogs.</h1>
        </Layout>
    );
}

export default BlogsPage;
